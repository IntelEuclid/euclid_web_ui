define(['jquery',
        'underscore'
], function ($, _) {
    var GlobalSettings = {
        timeoutInterval: 30000,
        system_ip: '10.42.0.1',
        debug: true,
    };
    return GlobalSettings;
});
